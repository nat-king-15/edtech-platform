# Production Environment Variables for EdTech Platform

# Node Environment
NODE_ENV=production
PORT=5000

# Firebase Configuration (will be automatically available in Firebase Functions)
# FIREBASE_PROJECT_ID will be set automatically
# FIREBASE_CLIENT_EMAIL will be set automatically
# FIREBASE_PRIVATE_KEY will be set automatically

# Email Configuration
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password
EMAIL_FROM=noreply@yourdomain.com

# Mux Configuration
MUX_TOKEN_ID=your-mux-token-id
MUX_TOKEN_SECRET=your-mux-token-secret
MUX_WEBHOOK_SECRET=your-mux-webhook-secret
MUX_WEBHOOK_URL=https://your-project-id.web.app/api/webhooks/mux

# Razorpay Configuration
RAZORPAY_KEY_ID=your-razorpay-key-id
RAZORPAY_KEY_SECRET=your-razorpay-key-secret

# JWT Configuration
JWT_SECRET=your-super-secure-jwt-secret-key-for-production
JWT_EXPIRES_IN=7d

# Redis Configuration (if using external Redis)
# REDIS_URL=redis://your-redis-url

# AWS S3 Configuration (if using S3)
# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key
# AWS_REGION=us-east-1
# S3_BUCKET_NAME=your-s3-bucket

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# CORS Configuration
CORS_ORIGIN=https://your-project-id.web.app

# Logging
LOG_LEVEL=info