<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{subject}} - {{platformName}}</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
            background-color: #f4f4f4;
        }
        .container {
            background-color: #ffffff;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        .header {
            text-align: center;
            border-bottom: 3px solid #007bff;
            padding-bottom: 20px;
            margin-bottom: 30px;
        }
        .header.success {
            border-bottom-color: #28a745;
        }
        .header.warning {
            border-bottom-color: #ffc107;
        }
        .header.error {
            border-bottom-color: #dc3545;
        }
        .header.info {
            border-bottom-color: #17a2b8;
        }
        .logo {
            font-size: 24px;
            font-weight: bold;
            color: #007bff;
            margin-bottom: 10px;
        }
        .logo.success {
            color: #28a745;
        }
        .logo.warning {
            color: #ffc107;
        }
        .logo.error {
            color: #dc3545;
        }
        .logo.info {
            color: #17a2b8;
        }
        .title {
            color: #2c3e50;
            font-size: 28px;
            margin-bottom: 10px;
        }
        .subtitle {
            color: #7f8c8d;
            font-size: 16px;
        }
        .content {
            margin-bottom: 30px;
        }
        .notification-box {
            background-color: #f8f9fa;
            padding: 20px;
            border-radius: 8px;
            border-left: 4px solid #007bff;
            margin: 20px 0;
        }
        .notification-box.success {
            background-color: #d4edda;
            border-left-color: #28a745;
        }
        .notification-box.warning {
            background-color: #fff3cd;
            border-left-color: #ffc107;
        }
        .notification-box.error {
            background-color: #f8d7da;
            border-left-color: #dc3545;
        }
        .notification-box.info {
            background-color: #d1ecf1;
            border-left-color: #17a2b8;
        }
        .detail-row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 10px;
            padding: 8px 0;
            border-bottom: 1px solid #e9ecef;
        }
        .detail-row:last-child {
            border-bottom: none;
        }
        .detail-label {
            font-weight: 600;
            color: #495057;
            min-width: 120px;
        }
        .detail-value {
            color: #212529;
            flex: 1;
            text-align: right;
        }
        .cta-button {
            display: inline-block;
            background-color: #007bff;
            color: white;
            padding: 12px 30px;
            text-decoration: none;
            border-radius: 5px;
            font-weight: 600;
            margin: 20px 0;
            text-align: center;
        }
        .cta-button:hover {
            background-color: #0056b3;
        }
        .cta-button.success {
            background-color: #28a745;
        }
        .cta-button.success:hover {
            background-color: #1e7e34;
        }
        .cta-button.warning {
            background-color: #ffc107;
            color: #212529;
        }
        .cta-button.warning:hover {
            background-color: #e0a800;
        }
        .cta-button.error {
            background-color: #dc3545;
        }
        .cta-button.error:hover {
            background-color: #c82333;
        }
        .footer {
            text-align: center;
            padding-top: 20px;
            border-top: 1px solid #e9ecef;
            color: #6c757d;
            font-size: 14px;
        }
        .footer a {
            color: #007bff;
            text-decoration: none;
        }
        .attachments {
            background-color: #f8f9fa;
            padding: 15px;
            border-radius: 5px;
            margin: 20px 0;
        }
        .attachment-item {
            display: flex;
            align-items: center;
            padding: 8px 0;
            border-bottom: 1px solid #e9ecef;
        }
        .attachment-item:last-child {
            border-bottom: none;
        }
        .attachment-icon {
            margin-right: 10px;
            color: #007bff;
        }
        @media (max-width: 600px) {
            body {
                padding: 10px;
            }
            .container {
                padding: 20px;
            }
            .detail-row {
                flex-direction: column;
            }
            .detail-value {
                text-align: left;
                margin-top: 5px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header {{type}}">
            <div class="logo {{type}}">{{platformName}}</div>
            <h1 class="title">{{subject}}</h1>
            {{#if subtitle}}
            <p class="subtitle">{{subtitle}}</p>
            {{/if}}
        </div>

        <div class="content">
            {{#if recipientName}}
            <p>Dear {{capitalize recipientName}},</p>
            {{else}}
            <p>Hello,</p>
            {{/if}}
            
            {{#if preMessage}}
            <p>{{preMessage}}</p>
            {{/if}}

            {{#if message}}
            <div class="notification-box {{type}}">
                <p style="margin: 0;">{{message}}</p>
            </div>
            {{/if}}

            {{#if details}}
            <div style="margin: 20px 0;">
                {{#each details}}
                <div class="detail-row">
                    <span class="detail-label">{{@key}}:</span>
                    <span class="detail-value">{{this}}</span>
                </div>
                {{/each}}
            </div>
            {{/if}}

            {{#if content}}
            <div style="margin: 20px 0;">
                <div style="background-color: #f8f9fa; padding: 15px; border-radius: 5px;">
                    {{{content}}}
                </div>
            </div>
            {{/if}}

            {{#if attachments}}
            <div class="attachments">
                <h4 style="margin-top: 0; color: #495057;">Attachments:</h4>
                {{#each attachments}}
                <div class="attachment-item">
                    <span class="attachment-icon">📎</span>
                    <a href="{{url}}" style="color: #007bff; text-decoration: none;">{{name}}</a>
                    {{#if size}}
                    <span style="margin-left: auto; color: #6c757d; font-size: 12px;">({{size}})</span>
                    {{/if}}
                </div>
                {{/each}}
            </div>
            {{/if}}

            {{#if actionUrl}}
            <div style="text-align: center; margin: 30px 0;">
                <a href="{{actionUrl}}" class="cta-button {{type}}">{{actionText}}</a>
            </div>
            {{/if}}

            {{#if additionalInfo}}
            <div style="background-color: #e9ecef; padding: 15px; border-radius: 5px; margin: 20px 0;">
                <h4 style="margin-top: 0; color: #495057;">Additional Information:</h4>
                <p style="margin-bottom: 0;">{{additionalInfo}}</p>
            </div>
            {{/if}}

            {{#if postMessage}}
            <p>{{postMessage}}</p>
            {{/if}}

            <p>Best regards,<br>
            <strong>{{platformName}} Team</strong></p>
        </div>

        <div class="footer">
            <p>This is an automated message from {{platformName}}.</p>
            {{#if unsubscribeUrl}}
            <p><a href="{{unsubscribeUrl}}">Unsubscribe from these notifications</a></p>
            {{/if}}
            <p>If you need assistance, contact us at <a href="mailto:{{supportEmail}}">{{supportEmail}}</a></p>
            <p>&copy; {{currentYear}} {{platformName}}. All rights reserved.</p>
        </div>
    </div>
</body>
</html>